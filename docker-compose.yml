services:
  wealthwise:
    build: .
    ports:
      - "8000:8000"
    environment:
      - USE_REAL_PROVIDERS=false
    # for live data + keyed providers, provide a .env and uncomment:
    # env_file: .env
    # depends_on: [db]

  # Postgres run/audit store. Uncomment together with `depends_on` above and set
  #   RUN_STORE=postgres
  #   RUN_STORE_DSN=postgresql://wealthwise:wealthwise@db:5432/wealthwise
  # in the .env. The table is created on startup; no migration step.
  # Left commented so `docker compose up` still gives a one-container demo that
  # needs no database and no keys.
  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: wealthwise
  #     POSTGRES_PASSWORD: wealthwise
  #     POSTGRES_DB: wealthwise
  #   ports: ["5432:5432"]
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U wealthwise"]
  #     interval: 5s
  #     retries: 10
